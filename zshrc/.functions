function plrb () {
    git pull --rebase origin `git rev-parse --abbrev-ref HEAD`
}

function rzsh () {
    source $HOME/.zshrc
}

function update_master() {
  g ft && g ch master && plrb
}

function update_bundler() {
  update_mater && bundle install
}

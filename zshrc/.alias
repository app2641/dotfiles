alias alc="alc-cmd s"
alias c="clear"
alias ls="exa"
alias sch="ps ax | ag "

# git
alias g="hub"
alias greset="g reset --hard"
alias pullreq='g compare'
alias gch='git checkout $(git br | fzf)'

# ghq
alias hb='hub browse $(ghq list | fxf | cut -d "/" -f 2,3)'
alias gb='hub browse'
alias gcd='cd $(ghq root)/$(ghq list | fxf)'

# ruby & rails
alias be="bundle exec"
alias cap="bundle exec cap"
alias rk="be rake"
alias rs="be rspec -fd"
alias db:st="be rake db:migrate:status"
alias db:up="be rake db:migrate"
alias db:down="be rake db:rollback"
alias db:redo="be rake db:migrate:redo"

# tmux
alias t='tmux'
alias tls='tmux ls'
alias ta='tmux attach-session -t '

# fzf
alias f='fzf'

# docker
alias dc='docker-compose'

# bat
alias cat='bat'
